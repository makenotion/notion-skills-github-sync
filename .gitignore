node_modules/
.env
.env.local
*.log
.DS_Store
dist/
.vercel/

# local secret files for `gh secret set` — never committed
.secrets/

# local config file with Notion database IDs — copy from config.json.example
config.json
